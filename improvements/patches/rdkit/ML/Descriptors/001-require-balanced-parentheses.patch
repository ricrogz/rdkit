diff --git a/rdkit/ML/Descriptors/Parser.py b/rdkit/ML/Descriptors/Parser.py
--- a/rdkit/ML/Descriptors/Parser.py
+++ b/rdkit/ML/Descriptors/Parser.py
@@ -279,5 +279,5 @@ def ParseCompoundDescriptor(cExpr, knownMethods):
             parenCount = parenCount + 1
           p = p + 1
-        if p <= len(res):
+        if not parenCount:
           res = res[0:start] + "'%s',compos,atomDict" % (res[start:p - 1]) + res[p - 1:]
   return res
